import numpy as np

# Creating an array
arr = np.array([5, 10, 15, 20, 25])

# Finding the maximum, minimum, and mean
max_val = np.max(arr)
min_val = np.min(arr)
mean_val = np.mean(arr)

print("Maximum Value:", max_val)
print("Minimum Value:", min_val)
print("Mean Value:", mean_val)
